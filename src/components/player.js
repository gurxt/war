import React from 'react'
import PlayerCSS from './../assets/styled/player-css.js'

const CSS = PlayerCSS()

const Player = () => {
    return (
        <CSS.Container>
        </CSS.Container>
    )
}

export default Player
